# Introduction

Most higher-order sequence words hand the quotation one element
at a time. The `*-index` family hands it the element *and* its
position. `times` runs a counted loop where the body has no
sequence at all — useful when you only need a tally.
