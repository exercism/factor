# Instructions append

## Errors

Throw one of these strings (verbatim) when the input is invalid:

- `"input base must be >= 2"`
- `"output base must be >= 2"`
- `"all digits must satisfy 0 <= d < input base"`

Negative numbers are not supported.
