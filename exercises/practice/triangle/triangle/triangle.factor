USING: kernel ;
IN: triangle

: equilateral? ( a b c -- ? )
    "unimplemented" throw ;

: isosceles? ( a b c -- ? )
    "unimplemented" throw ;

: scalene? ( a b c -- ? )
    "unimplemented" throw ;
