# Instructions append

## Words

`classify ( n -- str )` returns one of the strings `"perfect"`,
`"abundant"`, or `"deficient"`.

## Errors

When the input is zero or negative, throw the string

```
"Classification is only possible for positive integers."
```

(verbatim, including the trailing period).
